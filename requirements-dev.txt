# Dev dependencies — mirrors [project.optional-dependencies.dev] in pyproject.toml.
# Install with: pip install -e ".[dev]"
# Or directly:  pip install -r requirements-dev.txt

-r requirements.txt

pytest>=8.2
pytest-httpx>=0.30
pytest-cov>=5.0
ruff>=0.4
mypy>=1.10
